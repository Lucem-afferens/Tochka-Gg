# 🚀 Инструкция по автоматическому деплою

## ✅ Что уже настроено

- ✅ GitHub Actions workflow
- ✅ Автоматическая сборка файлов
- ✅ Деплой через FTP
- ✅ Путь: `/wp-content/themes/tochkagg-theme/`

---

## 📝 Что сделать сейчас

### 1. Создать папку на хостинге

В файловом менеджере Beget:
1. Зайдите в `wp-content/themes/`
2. Создайте папку: `tochkagg-theme`
3. Права: `755`

### 2. Проверить/запушить в GitHub

```bash
# Добавить файлы
git add .

# Коммит
git commit -m "Setup auto deploy"

# Push
git push
```

### 3. Проверить деплой

- Зайдите в GitHub → **Actions**
- Должен запуститься workflow "Deploy to Beget"
- Если ошибка - посмотрите логи и исправьте путь

---

## 🔧 Если путь неверный

Если деплой не работает, попробуйте изменить путь в `.github/workflows/deploy.yml`:

**Вариант 1** (текущий - если FTP в `public_html/`):
```yaml
server-dir: /wp-content/themes/tochkagg-theme/
```

**Вариант 2** (если FTP в корне домена):
```yaml
server-dir: /kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/
```

---

## ✅ После успешного деплоя

1. Зайдите в WordPress админку
2. **Внешний вид → Темы**
3. Найдите "Tochka Gg" → **Активировать**

---

## 🎯 Как это работает

При каждом `git push`:
1. ✅ Автоматически собираются файлы (`npm run build`)
2. ✅ Загружаются на хостинг
3. ✅ Обновляется тема

**Больше не нужно загружать файлы вручную!** 🎉

---

**Готово! Запустите первый деплой!**

