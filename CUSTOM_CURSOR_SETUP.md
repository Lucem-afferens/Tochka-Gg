# 🖱️ Настройка кастомного курсора

## 📋 Описание

Вы можете установить свой кастомный курсор для всего сайта вместо дефолтного браузерного. Курсор настраивается через WordPress админку в разделе "Настройки темы".

---

## 🎯 Шаг 1: Подготовка изображения курсора

### Требования к изображению:
- **Формат**: PNG (с прозрачностью) или SVG
- **Размер**: Рекомендуется 32x32px или 48x48px (максимум 128x128px)
- **Файл должен быть небольшим**: до 50KB для быстрой загрузки
- **Прозрачный фон**: PNG с альфа-каналом

### Где найти/создать курсор:
1. **Готовые курсоры**: 
   - [Cursor.cc](https://www.cursor.cc/)
   - [Open Cursor Library](https://www.openclipart.org/)
   - [Flaticon](https://www.flaticon.com/) (иконки в формате PNG)

2. **Создать свой**:
   - Используйте Photoshop, Figma, или любой графический редактор
   - Создайте изображение 32x32px с прозрачным фоном
   - Сохраните как PNG-24 с прозрачностью

---

## 🔧 Шаг 2: Добавление поля в SCF

### Вариант 1: Через интерфейс SCF (рекомендуется)

1. Перейдите в **SCF** → **Полевые группы**
2. Найдите группу **"Настройки темы (Options Page)"**
3. Нажмите **"Редактировать"**
4. Нажмите **"Добавить поле"** в конце списка полей
5. Заполните поля:

   **Основные настройки:**
   - **Метка**: `Кастомный курсор`
   - **Имя**: `custom_cursor`
   - **Тип поля**: `Image`

   **Настройки изображения:**
   - **Return Format**: `Image Array` (рекомендуется) или `Image URL`
   - **Preview Size**: `Thumbnail (150 x 150)`
   - **Library**: `all`

   **Инструкции:**
   ```
   Загрузите изображение курсора (PNG с прозрачностью, 32x32px, до 50KB).
   Рекомендуемый размер: 32x32px или 48x48px.
   Формат: PNG с прозрачным фоном.
   ```

6. Нажмите **"Сохранить"**

---

### Вариант 2: Через экспорт/импорт JSON (для продвинутых)

Если у вас уже есть экспортированная группа полей, добавьте это поле в JSON:

```json
{
  "key": "field_custom_cursor",
  "label": "Кастомный курсор",
  "name": "custom_cursor",
  "type": "image",
  "instructions": "Загрузите изображение курсора (PNG с прозрачностью, 32x32px, до 50KB). Рекомендуемый размер: 32x32px или 48x48px.",
  "required": 0,
  "return_format": "array",
  "preview_size": "thumbnail",
  "library": "all"
}
```

---

## 📤 Шаг 3: Загрузка курсора

1. Перейдите в **Настройки темы** (в левом меню WordPress)
2. Найдите поле **"Кастомный курсор"**
3. Нажмите **"Добавить изображение"** или **"Выбрать изображение"**
4. Загрузите ваш PNG файл с курсором
5. Нажмите **"Сохранить изменения"** внизу страницы

---

## ✅ Шаг 4: Проверка

1. Откройте сайт в браузере
2. Проверьте, что курсор изменился на ваш кастомный
3. Проверьте на разных элементах:
   - Обычный курсор (при наведении на текст)
   - Pointer курсор (при наведении на ссылки и кнопки)
   - Text курсор (при наведении на поля ввода)

---

## 🎨 Дополнительные настройки

### Изменение курсора для конкретных элементов

Если нужно изменить курсор только для определенных элементов, можно добавить CSS в **Внешний вид** → **Настроить** → **Дополнительный CSS**:

```css
/* Курсор только для ссылок */
a {
    cursor: url('URL_ВАШЕГО_КУРСОРА'), pointer !important;
}

/* Курсор для кнопок */
button, .tgg-button {
    cursor: url('URL_ВАШЕГО_КУРСОРА'), pointer !important;
}
```

**Но лучше использовать поле в Options Page** — так курсор будет применяться автоматически ко всем элементам.

---

## 🔄 Удаление кастомного курсора

Чтобы вернуть дефолтный курсор:

1. Перейдите в **Настройки темы**
2. Найдите поле **"Кастомный курсор"**
3. Нажмите **"Удалить изображение"** (крестик на превью)
4. Нажмите **"Сохранить изменения"**

После этого сайт вернется к использованию дефолтного браузерного курсора.

---

## ⚠️ Важные замечания

### Производительность:
- ✅ Используйте маленькие изображения (до 50KB)
- ✅ Рекомендуемый размер: 32x32px или 48x48px
- ✅ Формат PNG с прозрачностью
- ❌ Не используйте большие изображения (более 128x128px)
- ❌ Не используйте анимированные GIF (не поддерживается)

### Совместимость:
- ✅ Работает во всех современных браузерах
- ✅ Поддерживает прозрачность (PNG)
- ⚠️ На мобильных устройствах курсор не отображается (это нормально)
- ⚠️ В некоторых браузерах курсор может быть ограничен размером 32x32px

### Fallback:
- Если изображение не загружено, используется дефолтный браузерный курсор
- Если изображение повреждено, браузер автоматически использует fallback

---

## 🎨 Примеры использования

### Игровой курсор (для игрового клуба):
- Крестик прицела
- Меч или клинок
- Иконка контроллера
- Неоновый указатель

### Кибер-курсор (для кибер-тематики):
- Неоновый круг
- Голографический указатель
- Пиксельный курсор
- Кибер-стрелка

### Минималистичный курсор:
- Простая точка
- Тонкая стрелка
- Геометрическая фигура

---

## 🐛 Решение проблем

### Курсор не отображается:
1. Проверьте, что изображение загружено в поле "Кастомный курсор"
2. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
3. Проверьте консоль браузера на ошибки (F12)
4. Убедитесь, что изображение в формате PNG

### Курсор отображается неправильно:
1. Проверьте размер изображения (рекомендуется 32x32px)
2. Убедитесь, что изображение имеет прозрачный фон
3. Попробуйте перезагрузить изображение

### Курсор слишком большой/маленький:
- Измените размер исходного изображения (32x32px оптимально)
- Браузеры могут масштабировать курсор, но лучше использовать правильный размер

---

## 📝 Технические детали

### Как это работает:
1. Изображение курсора загружается через SCF Options Page
2. PHP функция `tochkagg_custom_cursor()` выводит CSS в `<head>`
3. CSS применяет курсор ко всем элементам через `cursor: url()`
4. Браузер использует fallback (`auto`, `pointer`, `text`) если изображение не загрузилось

### Файлы:
- `theme/inc/theme-functions.php` - функция вывода курсора
- `src/sass/base/_reset.scss` - базовые стили (комментарий)

---

## ✅ Готово!

Теперь у вас есть кастомный курсор на сайте! 🎉

Если возникнут вопросы или проблемы, проверьте раздел "Решение проблем" выше.

