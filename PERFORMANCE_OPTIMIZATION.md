# Руководство по оптимизации производительности сайта

## ✅ Выполненные оптимизации

### CSS оптимизации
1. **Добавлен `will-change`** для анимируемых элементов (GPU-ускорение)
2. **Уменьшен `blur`** с 60px до 40px для эффекта тумана
3. **Убран `filter: brightness()`** из keyframes анимаций (заменен на `opacity` и `text-shadow`)
4. **Добавлен `transform: translateZ(0)`** для принудительного GPU-ускорения фоновых слоев
5. **Добавлена поддержка `prefers-reduced-motion`** для пользователей с ограничениями
6. **Оптимизированы анимации**: используются только `transform`, `opacity` и `text-shadow`

### JavaScript оптимизации
1. **Использование `requestAnimationFrame`** вместо `setTimeout` для синхронизации с рендерингом
2. **Добавлен debounce/throttle** для событий `resize` и `scroll`
3. **Оптимизирована синхронизация высоты карточек** с использованием `requestAnimationFrame`
4. **Добавлена проверка `prefers-reduced-motion`** перед инициализацией анимаций
5. **Использование `{ once: true }`** для автоматического удаления обработчиков событий
6. **Оптимизированы DOM-запросы** с кэшированием результатов

## 📋 Рекомендации по дальнейшей оптимизации

### 1. Оптимизация изображений

#### Текущее состояние
- Изображения загружаются в полном размере
- Нет lazy loading для изображений вне viewport
- Нет адаптивных размеров изображений

#### Рекомендации
```html
<!-- Добавить lazy loading для изображений -->
<img src="image.jpg" loading="lazy" alt="Description">

<!-- Использовать srcset для адаптивных изображений -->
<img 
  srcset="image-small.jpg 480w, image-medium.jpg 768w, image-large.jpg 1200w"
  sizes="(max-width: 480px) 100vw, (max-width: 768px) 50vw, 33vw"
  src="image-large.jpg"
  alt="Description"
  loading="lazy"
>

<!-- Использовать WebP формат с fallback -->
<picture>
  <source srcset="image.webp" type="image/webp">
  <img src="image.jpg" alt="Description" loading="lazy">
</picture>
```

**Плагины WordPress:**
- **Smush** или **ShortPixel** - автоматическая оптимизация изображений
- **WP Rocket** - lazy loading изображений
- **WebP Express** - автоматическая конвертация в WebP

### 2. Минификация и сжатие

#### Текущее состояние
- CSS и JS уже минифицируются через Vite
- Размер CSS: ~186 KB (gzip: 28.61 KB)
- Размер JS: ~104 KB (gzip: 30.62 KB)

#### Рекомендации
1. **Включить Gzip/Brotli сжатие** на сервере (обычно уже включено)
2. **Использовать CDN** для статических ресурсов
3. **Настроить кэширование** браузера:
   ```apache
   # .htaccess
   <IfModule mod_expires.c>
     ExpiresActive On
     ExpiresByType text/css "access plus 1 year"
     ExpiresByType application/javascript "access plus 1 year"
     ExpiresByType image/png "access plus 1 year"
     ExpiresByType image/jpg "access plus 1 year"
     ExpiresByType image/jpeg "access plus 1 year"
     ExpiresByType image/gif "access plus 1 year"
     ExpiresByType image/webp "access plus 1 year"
   </IfModule>
   ```

### 3. Оптимизация шрифтов

#### Текущее состояние
- Используется Google Fonts (Orbitron)
- Шрифты загружаются синхронно

#### Рекомендации
```html
<!-- Предзагрузка критических шрифтов -->
<link rel="preload" href="font.woff2" as="font" type="font/woff2" crossorigin>

<!-- Асинхронная загрузка Google Fonts -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">

<!-- Или использовать font-display: swap в CSS -->
@font-face {
  font-family: 'Orbitron';
  font-display: swap;
  src: url('font.woff2') format('woff2');
}
```

**Альтернатива:** Скачать шрифты и разместить локально для уменьшения запросов к внешним серверам.

### 4. Оптимизация WordPress

#### Плагины для производительности
1. **WP Rocket** (платный) или **W3 Total Cache** (бесплатный)
   - Кэширование страниц
   - Минификация HTML/CSS/JS
   - Lazy loading изображений
   - Оптимизация базы данных

2. **Autoptimize**
   - Объединение и минификация CSS/JS
   - Оптимизация HTML

3. **Query Monitor**
   - Отслеживание медленных запросов к БД
   - Анализ загружаемых плагинов

#### Оптимизация базы данных
```sql
-- Очистка ревизий постов (выполнять через phpMyAdmin)
DELETE FROM wp_posts WHERE post_type = "revision";

-- Очистка спама в комментариях
DELETE FROM wp_comments WHERE comment_approved = 'spam';

-- Оптимизация таблиц
OPTIMIZE TABLE wp_posts, wp_postmeta, wp_comments, wp_options;
```

**Плагин:** **WP-Optimize** - автоматическая очистка и оптимизация БД

### 5. Оптимизация сервера

#### PHP оптимизация
```ini
; php.ini
memory_limit = 256M
max_execution_time = 300
opcache.enable = 1
opcache.memory_consumption = 128
opcache.interned_strings_buffer = 8
opcache.max_accelerated_files = 10000
opcache.revalidate_freq = 2
```

#### Nginx конфигурация (если используется)
```nginx
# Включить gzip
gzip on;
gzip_vary on;
gzip_proxied any;
gzip_comp_level 6;
gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;

# Кэширование статических файлов
location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2|ttf|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 6. Мониторинг производительности

#### Инструменты для тестирования
1. **Google PageSpeed Insights** - https://pagespeed.web.dev/
2. **GTmetrix** - https://gtmetrix.com/
3. **WebPageTest** - https://www.webpagetest.org/
4. **Chrome DevTools** - Lighthouse вкладка

#### Метрики для отслеживания
- **FCP (First Contentful Paint)** - < 1.8s
- **LCP (Largest Contentful Paint)** - < 2.5s
- **FID (First Input Delay)** - < 100ms
- **CLS (Cumulative Layout Shift)** - < 0.1
- **TTI (Time to Interactive)** - < 3.8s

### 7. Дополнительные оптимизации CSS

#### Критический CSS
Вынести критический CSS (выше fold) в `<style>` тег в `<head>`:
```html
<head>
  <style>
    /* Критический CSS для первого экрана */
    .tgg-header { ... }
    .tgg-hero { ... }
  </style>
  <link rel="stylesheet" href="main.css" media="print" onload="this.media='all'">
</head>
```

#### Удаление неиспользуемого CSS
- Использовать **PurgeCSS** для удаления неиспользуемых стилей
- Анализировать использование классов через Chrome DevTools Coverage

### 8. Оптимизация JavaScript

#### Code Splitting
Разделить JS на чанки для загрузки по требованию:
```javascript
// Динамический импорт для модулей, которые не нужны сразу
const initMap = () => import('./modules/map.js').then(m => m.initMap());

// Загружать только при необходимости
if (document.getElementById('map')) {
  initMap();
}
```

#### Отложенная загрузка
```html
<!-- Отложить загрузку не критичных скриптов -->
<script src="analytics.js" defer></script>
<script src="chat-widget.js" defer></script>
```

### 9. Оптимизация видео

#### Текущее состояние
- Видео используются как фоновые элементы

#### Рекомендации
```html
<!-- Добавить preload="none" для фоновых видео -->
<video autoplay muted loop preload="none" poster="poster.jpg">
  <source src="video.webm" type="video/webm">
  <source src="video.mp4" type="video/mp4">
</video>

<!-- Использовать YouTube/Vimeo для больших видео -->
<!-- Они оптимизируют доставку автоматически -->
```

### 10. Мониторинг в реальном времени

#### Инструменты
1. **Google Analytics** - отслеживание производительности
2. **New Relic** или **Datadog** - мониторинг сервера
3. **Uptime Robot** - мониторинг доступности

## 🎯 Приоритеты оптимизации

### Высокий приоритет (быстрый эффект)
1. ✅ Оптимизация CSS анимаций (выполнено)
2. ✅ Оптимизация JavaScript (выполнено)
3. ⚠️ Оптимизация изображений (lazy loading, WebP)
4. ⚠️ Настройка кэширования WordPress
5. ⚠️ Оптимизация шрифтов (preload, font-display)

### Средний приоритет
1. Настройка CDN
2. Критический CSS
3. Code splitting для JS
4. Оптимизация базы данных

### Низкий приоритет (тонкая настройка)
1. Мониторинг производительности
2. A/B тестирование оптимизаций
3. Дополнительные метрики

## 📊 Ожидаемые результаты

После выполнения всех рекомендаций:
- **Улучшение FCP**: 20-30%
- **Улучшение LCP**: 30-40%
- **Улучшение TTI**: 25-35%
- **Уменьшение размера страницы**: 30-50%
- **Улучшение PageSpeed Score**: 15-25 баллов

## 🔧 Быстрые действия (можно сделать прямо сейчас)

1. **Установить плагин оптимизации** (WP Rocket или W3 Total Cache)
2. **Включить lazy loading** для изображений
3. **Оптимизировать изображения** через Smush или ShortPixel
4. **Настроить кэширование** в .htaccess
5. **Добавить preload** для критических шрифтов

## 📝 Примечания

- Все оптимизации должны тестироваться на реальных устройствах
- Использовать инкогнито режим для тестирования (без расширений)
- Тестировать на медленных соединениях (3G)
- Мониторить производительность регулярно (раз в месяц)

