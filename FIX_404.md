# Исправление ошибки 404 на страницах

## 🔍 Проблема
При переходе на другие страницы появляется ошибка 404.

## ✅ Решение

### Шаг 1: Настройка постоянных ссылок в WordPress

1. **Войдите в админку WordPress:**
   - `http://kungur-tochkagg.ru/wp-admin`

2. **Перейдите в настройки:**
   - Настройки → Постоянные ссылки

3. **Выберите формат ссылок:**
   - Выберите любой формат, **кроме** "Обычные"
   - Рекомендуется: **"Название записи"** или **"Произвольно"** с `/page/%postname%/`
   
4. **Нажмите "Сохранить изменения"**
   - WordPress автоматически обновит `.htaccess` файл

---

### Шаг 2: Проверка файла .htaccess

Файл `.htaccess` должен быть в корне WordPress:
- Путь: `/public_html/.htaccess` (или `/kungur-tochkagg.ru/public_html/.htaccess`)

**Убедитесь, что:**
- ✅ Файл существует
- ✅ В нем есть блок `# BEGIN WordPress ... # END WordPress`
- ✅ Права доступа: `644`

---

### Шаг 3: Если проблема остаётся

**Проверьте, созданы ли страницы:**
1. Страницы → Все страницы
2. Убедитесь, что нужные страницы созданы и опубликованы

**Очистите кеш:**
- Если используется кеш плагин - очистите его
- Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)

**Проверьте права на файл:**
- Убедитесь, что WordPress может писать в `.htaccess`
- Права на папку: `755`
- Права на файл: `644`

---

## ⚠️ Важно

Файл `.htaccess` должен быть в **корне WordPress**, а не в папке темы!

Правильное расположение:
```
/public_html/
├── .htaccess          ← здесь!
├── wp-config.php
├── wp-content/
│   └── themes/
│       └── tochkagg-theme/
└── ...
```

---

## 🔧 Альтернативное решение

Если автоматическое создание `.htaccess` не работает:

1. Вручную скопируйте содержимое блока WordPress из админки
2. Добавьте в начало файла `.htaccess` наши настройки (PHP, безопасность)
3. В конец добавьте блок WordPress

Или просто откройте: **Настройки → Постоянные ссылки** и нажмите **"Сохранить"** - WordPress автоматически обновит файл.

