# 🔍 Технический аудит производительности

**Дата:** 2025-01-XX  
**Цель:** Максимально повысить производительность без изменения дизайна и логики

---

## 🔴 CRITICAL (Критичные проблемы)

### 1. Принудительный cache bust с `time()` - ломает кеширование
**Файл:** `theme/inc/theme-functions.php:77, 84`  
**Проблема:** Использование `time()` в версии файла делает невозможным кеширование браузером  
**Влияние:** Каждый запрос загружает CSS/JS заново, увеличивает LCP  
**Решение:** Убрать `time()`, использовать только `filemtime()`  
**Приоритет:** 🔴 CRITICAL

```php
// ❌ ПЛОХО
$style_version = TOCHKAGG_THEME_VERSION . '.' . filemtime($style_path) . '.' . time();

// ✅ ХОРОШО
$style_version = TOCHKAGG_THEME_VERSION . '.' . filemtime($style_path);
```

---

### 2. Дублирование версий в enqueue-assets.php
**Файл:** `theme/inc/enqueue-assets.php:22, 37` и `theme/inc/theme-functions.php:77, 84`  
**Проблема:** Версии добавляются дважды - в `enqueue-assets.php` и в `cache_bust()`  
**Влияние:** Лишние вычисления, конфликты версий  
**Решение:** Убрать дублирование, оставить только в одном месте  
**Приоритет:** 🔴 CRITICAL

---

### 3. Layout Thrashing: множественные `offsetHeight` и `getBoundingClientRect`
**Файлы:**
- `src/js/modules/slider.js:67` - `offsetHeight` в цикле
- `src/js/modules/news-cards.js:23, 28` - `offsetHeight` в цикле
- `src/js/modules/navigation.js:196` - `getBoundingClientRect()` при скролле
- `src/js/modules/smooth-scroll.js:37-38` - `offsetHeight` и `getBoundingClientRect()`

**Проблема:** Чтение layout свойств вызывает синхронный reflow  
**Влияние:** Блокирует рендеринг, вызывает лаги при скролле  
**Решение:** Батчить чтения, кэшировать результаты, использовать `requestAnimationFrame`  
**Приоритет:** 🔴 CRITICAL

---

### 4. Тяжелый `filter: blur(40px)` на фоне
**Файл:** `src/sass/base/_reset.scss:135`  
**Проблема:** `filter: blur(40px)` на большом фоне - очень дорогая операция  
**Влияние:** Снижает FPS, особенно на мобильных  
**Решение:** Уменьшить blur до 20-25px или использовать `backdrop-filter` только где нужно  
**Приоритет:** 🔴 CRITICAL

---

## 🟡 HIGH (Высокий приоритет)

### 5. Множественные обработчики `resize` без debounce
**Файлы:**
- `src/js/modules/bar-accordion.js:134` - debounce 200ms
- `src/js/modules/news-cards.js:52` - debounce 500ms
- `src/js/modules/slider.js:123` - debounce 500ms
- `src/js/modules/animations.js:162` - debounce 250ms

**Проблема:** Каждый модуль создает свой обработчик resize  
**Влияние:** Множественные вызовы при изменении размера окна  
**Решение:** Создать единый менеджер resize событий с debounce  
**Приоритет:** 🟡 HIGH

---

### 6. Swiper загружается всегда, даже когда не используется
**Файл:** `src/js/main.js:11-19`  
**Проблема:** Swiper импортируется и добавляется в `window` на всех страницах  
**Влияние:** ~50KB JS загружается даже там, где нет слайдеров  
**Решение:** Динамический импорт Swiper только на страницах со слайдерами  
**Приоритет:** 🟡 HIGH

---

### 7. Бесконечные анимации с `will-change` на всех элементах
**Файлы:** Множественные использования в `src/sass/`  
**Проблема:** `will-change` создает отдельный слой композиции, потребляет память  
**Влияние:** Увеличивает потребление памяти, особенно на мобильных  
**Решение:** Использовать `will-change` только для активно анимируемых элементов, убирать после анимации  
**Приоритет:** 🟡 HIGH

---

### 8. Множественные `setTimeout` без очистки
**Файлы:**
- `src/js/modules/news-cards.js:42, 45` - setTimeout без проверки
- `src/js/modules/vr-modal.js:52` - setTimeout может не очиститься
- `src/js/modules/booking.js:77, 117` - setTimeout в обработчиках

**Проблема:** Таймеры могут накапливаться, если компоненты переинициализируются  
**Влияние:** Утечки памяти, лишние вызовы функций  
**Решение:** Всегда очищать таймеры при размонтировании/переинициализации  
**Приоритет:** 🟡 HIGH

---

### 9. Изображения без `loading="lazy"` в некоторых местах
**Файлы:**
- `theme/template-parts/components/tournaments-preview.php:341` - нет lazy loading
- `theme/archive-tournament.php:59` - `the_post_thumbnail()` без lazy loading

**Проблема:** Изображения загружаются сразу, даже вне viewport  
**Влияние:** Увеличивает начальный размер страницы, замедляет LCP  
**Решение:** Добавить `loading="lazy"` для всех изображений вне первого экрана  
**Приоритет:** 🟡 HIGH

---

### 10. Дорогие CSS свойства: множественные `box-shadow` и `text-shadow`
**Файлы:** Множественные использования в `src/sass/`  
**Проблема:** Многослойные тени (3-6 слоев) - дорогие для рендеринга  
**Влияние:** Снижает FPS, особенно при анимациях  
**Решение:** Уменьшить количество слоев до 2-3 максимум, использовать более простые тени  
**Приоритет:** 🟡 HIGH

---

## 🟠 MEDIUM (Средний приоритет)

### 11. `backdrop-filter` на десктопе (уже частично оптимизировано)
**Файл:** `src/sass/sections/_header.scss:27, 85`  
**Проблема:** `backdrop-filter: blur()` - дорогая операция  
**Влияние:** Снижает производительность на слабых устройствах  
**Решение:** Уже частично отключено на десктопе, можно полностью убрать где не критично  
**Приоритет:** 🟠 MEDIUM

---

### 12. Множественные `addEventListener` на `document` без удаления
**Файлы:**
- `src/js/modules/vr-modal.js:64` - `keydown` на document
- `src/js/modules/navigation.js:90, 99, 107` - множественные обработчики

**Проблема:** Обработчики остаются в памяти после размонтирования  
**Влияние:** Потенциальные утечки памяти при навигации  
**Решение:** Удалять обработчики при размонтировании компонентов  
**Приоритет:** 🟠 MEDIUM

---

### 13. Синхронизация высоты карточек вызывает reflow
**Файлы:**
- `src/js/modules/news-cards.js:8-35`
- `src/js/modules/slider.js:38-98`

**Проблема:** Установка `height: auto` и последующее чтение `offsetHeight` вызывает reflow  
**Влияние:** Блокирует рендеринг при изменении размера окна  
**Решение:** Использовать CSS Grid/Flexbox вместо JS синхронизации, или батчить операции  
**Приоритет:** 🟠 MEDIUM

---

### 14. Избыточное использование `!important`
**Файлы:** 60 использований в `src/sass/`  
**Проблема:** `!important` усложняет каскад, увеличивает специфичность  
**Влияние:** Усложняет поддержку, может вызывать конфликты  
**Решение:** Убрать `!important` где возможно, использовать более специфичные селекторы  
**Приоритет:** 🟠 MEDIUM

---

### 15. Кастомный курсор: `will-change: transform` всегда активен
**Файл:** `theme/inc/theme-functions.php:278`  
**Проблема:** `will-change` создает отдельный слой композиции постоянно  
**Влияние:** Потребляет память даже когда курсор не двигается  
**Решение:** Добавлять `will-change` только при движении, убирать после паузы  
**Приоритет:** 🟠 MEDIUM

---

## 🟢 LOW (Низкий приоритет)

### 16. Избыточные проверки в циклах
**Файлы:** Множественные `forEach` с проверками внутри  
**Проблема:** Проверки выполняются на каждой итерации  
**Влияние:** Минимальное, но можно оптимизировать  
**Решение:** Фильтровать массив перед циклом  
**Приоритет:** 🟢 LOW

---

### 17. Дублирование кода в обработчиках событий
**Файлы:** `src/js/modules/booking.js:56-67, 96-107`  
**Проблема:** Похожий код для iOS и Android  
**Влияние:** Увеличивает размер бандла  
**Решение:** Вынести общую логику в функцию  
**Приоритет:** 🟢 LOW

---

### 18. Неиспользуемые импорты
**Файл:** `src/js/main.js:24` - `initParallax` импортируется, но не используется  
**Проблема:** Мертвый код увеличивает размер бандла  
**Влияние:** Минимальное  
**Решение:** Удалить неиспользуемые импорты  
**Приоритет:** 🟢 LOW

---

## 📊 Статистика проблем

- **CRITICAL:** 4 проблемы
- **HIGH:** 6 проблем  
- **MEDIUM:** 5 проблем
- **LOW:** 3 проблемы

**Всего:** 18 проблем

---

## 🎯 Рекомендуемый порядок исправлений

1. **Фаза 1 (CRITICAL):**
   - Убрать `time()` из версий файлов
   - Убрать дублирование версий
   - Оптимизировать layout thrashing
   - Уменьшить `filter: blur`

2. **Фаза 2 (HIGH):**
   - Единый менеджер resize
   - Динамический импорт Swiper
   - Оптимизация `will-change`
   - Очистка таймеров
   - Lazy loading изображений
   - Упрощение теней

3. **Фаза 3 (MEDIUM + LOW):**
   - Остальные оптимизации

---

## 📈 Ожидаемый эффект

После исправления CRITICAL и HIGH проблем:
- **LCP:** улучшение на 20-30%
- **FPS:** стабильные 60fps на десктопе
- **Размер JS:** уменьшение на ~50KB (Swiper)
- **Время рендера:** уменьшение на 15-25%

