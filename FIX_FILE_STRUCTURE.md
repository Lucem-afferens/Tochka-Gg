# 🔧 Исправление структуры файлов

## ❌ Проблема

1. Файл `style.css` лежит в корне темы: `tochkagg-theme/style.css`
2. Размер файла всего **0.3 KB** (должно быть **53 KB**)
3. Должно быть: `tochkagg-theme/assets/css/style.css`

---

## ✅ Решение

### Шаг 1: Создайте структуру папок на сервере

**Через файловый менеджер Beget:**

1. Зайдите в: `kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/`
2. Создайте папку `assets/` (права: 755)
3. Внутри `assets/` создайте папку `css/` (права: 755)
4. Внутри `assets/` создайте папку `js/` (права: 755)

**Итоговая структура:**
```
tochkagg-theme/
  ├── assets/
  │   ├── css/
  │   └── js/
  ├── functions.php
  └── style.css (это заголовок темы WordPress - оставьте!)
```

---

### Шаг 2: Удалите неправильный файл

Если в корне темы есть файл `style.css` размером 0.3 KB:
- Это не тот файл - удалите его или переместите

**Важно:** Не удаляйте файл `style.css` в корне темы, если он содержит заголовок темы WordPress (первые строки с комментарием о теме). Это разные файлы!

---

### Шаг 3: Сделайте новый деплой

После создания папок сделайте push в GitHub - файлы должны загрузиться в правильные места.

---

## 📁 Правильная структура

После деплоя должно быть:

```
tochkagg-theme/
  ├── assets/
  │   ├── css/
  │   │   └── style.css (53 KB) ✅
  │   └── js/
  │       └── main.js ✅
  ├── functions.php
  ├── style.css (заголовок темы WordPress)
  └── ...
```

---

**Создайте папки на сервере и сообщите, когда готово - сделаем деплой!**

