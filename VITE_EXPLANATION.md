# Объяснение: Почему 404 в Vite для WordPress темы

## 🤔 Проблема

После запуска `npm run dev` вы видите 404 на `http://localhost:3000`.

## ✅ Это нормально!

**404 это нормальное поведение для WordPress темы!** Вот почему:

### Как работает Vite для WordPress темы

1. **Vite компилирует файлы:**
   - `src/sass/style.scss` → `theme/assets/css/style.css`
   - `src/js/main.js` → `theme/assets/js/main.js`

2. **Vite dev сервер:**
   - Нужен для hot reload (автоматическая пересборка)
   - Но НЕ показывает сам сайт
   - Показывает 404, потому что нет HTML файла

3. **WordPress показывает сайт:**
   - Открываете WordPress в браузере
   - Там видны все изменения после компиляции

---

## 🎯 Два способа работы

### Способ 1: Только компиляция (рекомендуется)

**Запускаете:**
```bash
npm run dev
```

**Что происходит:**
- ✅ Vite компилирует файлы в `theme/assets/`
- ✅ Автоматически пересобирает при изменениях
- ⚠️ Показывает 404 на localhost:3000 (это нормально!)
- ✅ Файлы готовы для WordPress

**Используете:**
- Открываете WordPress в браузере (если есть)
- Или просто работаете с файлами, они компилируются

---

### Способ 2: Тестовая страница

Я создал `index.html` в корне проекта - теперь можно открыть `http://localhost:3000` и увидеть тестовую страницу для проверки стилей.

**Но это только для тестирования стилей!**

Для полной работы нужен WordPress.

---

## ✅ Проверка что всё работает

### 1. Проверьте что файлы компилируются

После запуска `npm run dev` проверьте:

```bash
ls -la theme/assets/css/
ls -la theme/assets/js/
```

Должны появиться файлы:
- `theme/assets/css/style.css`
- `theme/assets/js/main.js`

### 2. Проверьте содержимое файлов

```bash
head theme/assets/css/style.css
```

Если файл не пустой - всё работает! ✅

---

## 🚀 Правильный workflow

### Для разработки фронтенда:

1. **Запускаете Vite:**
   ```bash
   npm run dev
   ```

2. **Редактируете файлы:**
   - Изменяете `src/sass/style.scss`
   - Vite автоматически компилирует → `theme/assets/css/style.css`

3. **Проверяете результат:**
   - Открываете `theme/assets/css/style.css` - видите изменения
   - Или открываете тестовый `index.html` на localhost:3000

### Для полной разработки (с WordPress):

1. **Запускаете Vite** (в одном терминале)
   ```bash
   npm run dev
   ```

2. **Запускаете WordPress** (локальный сервер)

3. **Открываете WordPress в браузере:**
   - `http://tochkagg.local` или ваш адрес
   - Видите сайт с вашими стилями

---

## 📝 Важные моменты

### 404 на localhost:3000 - это нормально!

Vite dev сервер нужен для:
- ✅ Компиляции файлов
- ✅ Hot reload при изменениях
- ✅ Быстрой разработки

Но он НЕ показывает WordPress сайт!

### Для WordPress нужен отдельный сервер

WordPress требует:
- PHP сервер
- База данных
- Локальный сервер (XAMPP, MAMP, Local, Docker)

Vite только компилирует CSS/JS, но не запускает WordPress.

---

## 🎯 Что делать сейчас

### Вариант 1: Только фронтенд

1. Запустите `npm run dev`
2. Редактируйте `src/sass/` и `src/js/`
3. Проверяйте файлы в `theme/assets/`
4. Тестовая страница доступна на localhost:3000

### Вариант 2: Полная разработка

1. Запустите `npm run dev`
2. Установите локальный WordPress
3. Скопируйте тему в WordPress
4. Откройте WordPress в браузере

---

## ✅ Итог

**404 на localhost:3000 - это нормально!**

Vite работает правильно - он компилирует файлы. Для просмотра сайта нужен WordPress сервер.

**Проверьте что файлы компилируются:**
```bash
ls theme/assets/css/style.css
ls theme/assets/js/main.js
```

Если файлы есть - всё работает отлично! 🎉


