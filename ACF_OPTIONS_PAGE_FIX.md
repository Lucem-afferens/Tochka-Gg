# Как создать Options Page в ACF

## 🎯 Вариант 1: ACF Pro (рекомендуется)

Если у вас установлен **ACF Pro**:

1. В левом меню WordPress найдите **Custom Fields**
2. В подменю найдите **Options Pages**
3. Нажмите **"Add New"**
4. Заполните форму

## 🔧 Вариант 2: Если Options Pages не доступны (ACF Free)

Если в меню нет **Options Pages**, значит у вас установлена бесплатная версия ACF. 

### Решение: Создать Options Page через код

Добавьте следующий код в файл `theme/inc/theme-setup.php` или создайте новый файл `theme/inc/acf-options.php`:

```php
<?php
/**
 * Регистрация ACF Options Page
 * 
 * @package Tochkagg_Theme
 */

if (!defined('ABSPATH')) {
    exit;
}

/**
 * Добавление Options Page для ACF (если используется ACF Free)
 * 
 * Для ACF Pro это не требуется - Options Pages создаются через админку
 */
function tochkagg_add_acf_options_page() {
    // Проверяем наличие функции ACF Pro
    if (function_exists('acf_add_options_page')) {
        acf_add_options_page([
            'page_title'    => 'Настройки темы',
            'menu_title'    => 'Настройки темы',
            'menu_slug'     => 'theme-options',
            'capability'    => 'edit_posts',
            'icon_url'      => 'dashicons-admin-settings',
            'position'      => 2,
        ]);
    }
}
add_action('acf/init', 'tochkagg_add_acf_options_page');
```

### Затем подключите этот файл в `functions.php`:

```php
require_once TOCHKAGG_THEME_PATH . '/inc/acf-options.php';
```

---

## 📋 Пошаговая инструкция для ACF Pro

### Шаг 1: Создание Options Page

1. Войдите в админку WordPress
2. В левом меню найдите **Custom Fields**
3. Наведите курсор на **Custom Fields** - откроется подменю
4. Выберите **Options Pages**
5. Нажмите кнопку **"Add New"** (вверху страницы)
6. Заполните форму:
   - **Page Title:** `Настройки темы`
   - **Menu Slug:** `theme-options`
   - **Menu Title:** `Настройки темы` (или любой другой текст для меню)
   - **Capability:** `edit_posts` (по умолчанию)
   - **Position:** `2` (опционально - позиция в меню)
   - **Icon:** Выберите иконку из списка (опционально)
   - **Redirect:** (не отмечайте)
7. Нажмите **"Publish"** (вверху справа)

### Шаг 2: Проверка

После создания вы должны увидеть в левом меню WordPress новый пункт **"Настройки темы"** (или как вы назвали).

### Шаг 3: Создание группы полей

1. Перейдите в **Custom Fields → Field Groups**
2. Нажмите **"Add New"** или отредактируйте существующую группу
3. В разделе **Location Rules** добавьте правило:
   ```
   Show this field group if:
   Options Page → is equal to → theme-options
   ```
4. Добавьте нужные поля в группу
5. Нажмите **"Publish"**

### Шаг 4: Проверка работы

1. Перейдите в меню **"Настройки темы"** (которое вы создали)
2. Вы должны увидеть все поля, которые добавили в группу
3. Заполните поля и сохраните

---

## ⚠️ Важные моменты

### Если Options Pages нет в меню:

1. **Проверьте версию ACF:**
   - Перейдите в **Плагины**
   - Найдите Advanced Custom Fields
   - Посмотрите версию
   - ACF Pro имеет версию типа "6.x Pro"
   - ACF Free имеет версию типа "6.x"

2. **Если установлен ACF Free:**
   - Options Pages недоступны без кода (см. Вариант 2 выше)
   - Или купите/активируйте ACF Pro

3. **Проверьте права доступа:**
   - Убедитесь, что ваш пользователь имеет права администратора
   - Или права на редактирование постов (`edit_posts`)

### Альтернативные пути:

Если не видите **Custom Fields → Options Pages**, попробуйте:

1. **Прямая ссылка в админке:**
   ```
   /wp-admin/edit.php?post_type=acf-options-page
   ```

2. **Или через адресную строку:**
   ```
   /wp-admin/admin.php?page=acf-options-page
   ```

---

## 🔍 Как проверить, какая версия ACF установлена

1. Перейдите в **Плагины → Установленные плагины**
2. Найдите **Advanced Custom Fields**
3. Посмотрите версию:
   - **ACF Pro:** будет написано "Pro" и версия типа "6.3.0 Pro"
   - **ACF Free:** будет написано только версия типа "6.3.0"

---

## 📝 Резюме

**Для ACF Pro:**
- Custom Fields → Options Pages → Add New

**Для ACF Free:**
- Используйте код из "Вариант 2" выше
- Или приобретите ACF Pro

**После создания Options Page:**
- Создайте группу полей с Location Rule на эту Options Page
- Заполните поля через меню "Настройки темы"

