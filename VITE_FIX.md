# ✅ Исправление: Vite для WordPress темы

## 🔍 Проблема

После `npm run dev` показывается 404 на localhost:3000.

## ✅ Решение

**404 - это нормально!** Но теперь я исправил конфигурацию.

### Что изменилось:

1. **Создан тестовый `index.html`** - теперь можно открыть localhost:3000 для тестирования стилей
2. **Обновлена команда `dev`** - теперь она компилирует файлы в watch режиме (автоматическая пересборка)

---

## 🚀 Как использовать

### Команда разработки:

```bash
npm run dev
```

**Теперь Vite будет:**
- ✅ Компилировать файлы в `theme/assets/`
- ✅ Автоматически пересобирать при изменениях (watch режим)
- ✅ Показывать тестовую страницу на localhost:3000 (для проверки стилей)

---

## 📁 Что происходит

### При запуске `npm run dev`:

1. **Vite компилирует:**
   - `src/sass/style.scss` → `theme/assets/css/style.css`
   - `src/js/main.js` → `theme/assets/js/main.js`

2. **Файлы готовы:**
   - Можно использовать в WordPress
   - Можно проверить на тестовой странице (localhost:3000)

3. **Watch режим:**
   - При изменении файлов → автоматическая пересборка
   - Не нужно перезапускать команду

---

## 🎯 Два способа проверки

### Способ 1: Тестовая страница

1. Запустите `npm run dev`
2. Откройте `http://localhost:3000`
3. Увидите тестовую страницу со стилями
4. Редактируйте `src/sass/` → видите изменения после пересборки

### Способ 2: WordPress

1. Запустите `npm run dev` (в фоне)
2. Откройте WordPress в браузере
3. Видите сайт с вашими стилями
4. Редактируете файлы → они автоматически компилируются

---

## ✅ Проверка

### После запуска `npm run dev`:

```bash
# Проверить что файлы скомпилировались
ls -la theme/assets/css/
ls -la theme/assets/js/
```

Должны появиться:
- `theme/assets/css/style.css` (не пустой)
- `theme/assets/js/main.js` (не пустой)

---

## 📝 Важно понимать

### Для WordPress темы:

- **Vite компилирует** CSS/JS → файлы в `theme/assets/`
- **WordPress использует** эти файлы через `functions.php`
- **404 на localhost:3000** - нормально, если нет HTML (но теперь есть тестовый!)
- **Тестовая страница** - только для проверки стилей, не полный WordPress

### Для полной работы:

Нужен локальный WordPress сервер, но для разработки фронтенда его не обязательно иметь сразу.

---

## 🎉 Всё готово!

Теперь просто:

```bash
npm run dev
```

И всё работает! 🚀

Файлы компилируются, тестовая страница доступна на localhost:3000.


