# 🔧 Исправление ошибки поля URL в SCF

## Проблема
Ошибка: **"Значение должно быть допустимым URL"** при попытке использовать якорь `#about` в поле типа URL.

---

## ✅ Решение 1: Использовать тип поля "Text" (Рекомендуется)

Если нужно использовать якоря (например, `#about`), лучше использовать тип поля **"Text"** вместо **"URL"**.

### Как изменить:

1. Откройте группу полей **"Главная страница — Hero секция"**
2. Найдите поле **"Ссылка кнопки CTA Hero"** (`hero_cta_link`)
3. Измените **"Тип поля"** с **"URL"** на **"Text"**
4. **Значение по умолчанию**: Введите `#about`
5. Сохраните группу полей

Теперь можно вводить:
- Якоря: `#about`
- Относительные URL: `/equipment`
- Полные URL: `https://example.com/page`

---

## ✅ Решение 2: Оставить тип URL и использовать полный URL

Если оставляете тип **"URL"**, нужно вводить полный URL:

### При заполнении страницы:

1. Откройте страницу "Главная" для редактирования
2. В поле **"Ссылка кнопки CTA Hero"** введите:
   ```
   https://ваш-сайт.ru/#about
   ```
   
   Замените `ваш-сайт.ru` на реальный домен вашего сайта.

3. Или введите полный URL без якоря:
   ```
   https://ваш-сайт.ru/
   ```

---

## ✅ Решение 3: Оставить поле пустым и использовать значение по умолчанию в коде

1. В настройках группы полей удалите **"Значение по умолчанию"** из поля URL (оставьте пустым)
2. При заполнении страницы оставьте поле пустым
3. В коде темы значение по умолчанию уже установлено (`#about`), поэтому оно будет использоваться автоматически

---

## 🎯 Рекомендация

**Используйте Решение 1** (тип поля "Text"):
- ✅ Позволяет использовать якоря (`#about`)
- ✅ Позволяет использовать относительные URL (`/page`)
- ✅ Позволяет использовать полные URL (`https://example.com`)
- ✅ Более гибкое решение

---

## 📝 Для поля "Ссылка кнопки CTA Hero"

### Вариант А: Тип "Text" (рекомендуется)

- **Тип поля**: **Text**
- **Метка**: `Ссылка кнопки CTA Hero`
- **Имя**: `hero_cta_link`
- **Значение по умолчанию**: `#about`
- **Инструкции**: `Ссылка или якорь для кнопки CTA (например: #about или https://example.com)`

### Вариант Б: Тип "URL"

- **Тип поля**: **URL**
- **Метка**: `Ссылка кнопки CTA Hero`
- **Имя**: `hero_cta_link`
- **Значение по умолчанию**: Оставьте **пустым**
- **Протокол**: Выберите **"относительный"** (чтобы разрешить якоря, если SCF это поддерживает)
- **Инструкции**: `Полный URL или якорь (например: https://ваш-сайт.ru/#about)`

При заполнении страницы введите полный URL с якорем.

---

## ❓ Почему возникает ошибка?

Поле типа **"URL"** в SCF валидирует значение и требует правильный формат URL. Якорь `#about` технически не является валидным URL (отсутствует протокол и домен).

---

**Какой вариант выбрать?** Используйте **Вариант А (Text)** — это проще и гибче.

