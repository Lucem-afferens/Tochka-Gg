# 🚀 Быстрый старт

## Можно ли использовать Vite для WordPress темы?

**Да, можно!** Но нужен комбинированный подход:

### ✅ Что работает через Vite:
- SASS → CSS компиляция
- JavaScript модули
- Hot reload при изменении CSS/JS
- Оптимизация ресурсов

### ⚠️ Что требует WordPress сервер:
- PHP шаблоны
- ACF поля и данные
- Динамический контент
- WordPress функции

---

## 🎯 Два варианта работы

### Вариант 1: Полная разработка (рекомендуется)

**Нужно:**
- Локальный WordPress (Local, XAMPP, MAMP, Docker)
- Vite dev сервер

**Как работает:**
1. Запускаете WordPress локально (`http://tochkagg.local`)
2. Запускаете Vite (`npm run dev`)
3. Vite компилирует SASS/JS → файлы попадают в тему
4. Открываете WordPress в браузере → видите изменения

**Преимущества:**
- ✅ Полная функциональность WordPress
- ✅ Работа с ACF
- ✅ Динамический контент
- ✅ Hot reload для CSS/JS

### Вариант 2: Только фронтенд

**Нужно:**
- Только Vite

**Как работает:**
1. Запускаете Vite (`npm run dev`)
2. Разрабатываете только стили и JS
3. Потом переносите в WordPress

**Ограничения:**
- ❌ Нет PHP
- ❌ Нет ACF
- ❌ Только статическая верстка

---

## 📦 Установка

### 1. Установить зависимости

```bash
npm install
```

### 2. Настроить пути в `vite.config.wordpress.mjs`

Откройте файл и измените путь к теме:

```javascript
// Если тема в WordPress
const WORDPRESS_THEME_PATH = resolve(__dirname, '../wp-content/themes/tochkagg-theme');

// Или если тема в этом проекте
const LOCAL_THEME_PATH = resolve(__dirname, 'theme');
```

### 3. Запустить разработку

```bash
# Терминал 1: Vite dev сервер
npm run dev

# Vite будет:
# - Компилировать SASS → CSS
# - Компилировать JS
# - Выводить файлы в theme/assets/
# - Автоматически пересобирать при изменениях
```

---

## 🎨 Процесс разработки

### Шаг 1: Запуск

```bash
# Терминал 1 - Vite
npm run dev

# Откроется на http://localhost:3000
# Но вы будете работать через WordPress
```

### Шаг 2: Открыть WordPress

Откройте ваш локальный WordPress в браузере:
- `http://tochkagg.local`
- `http://localhost/tochkagg`
- Или другой ваш адрес

### Шаг 3: Редактирование

**Изменили SASS:**
1. Редактируете `src/sass/style.scss`
2. Vite автоматически компилирует → `theme/assets/css/style.css`
3. Обновляете страницу WordPress → видите изменения

**Изменили JavaScript:**
1. Редактируете `src/js/main.js`
2. Vite автоматически компилирует → `theme/assets/js/main.js`
3. Обновляете страницу WordPress → видите изменения

**Изменили PHP:**
1. Редактируете файлы в `theme/`
2. Обновляете страницу WordPress → видите изменения
3. Vite не нужен (это просто PHP)

---

## 📁 Структура файлов

### Во время разработки:

```
Tochka-Gg/
├── src/                    # Исходники (редактируете здесь)
│   ├── sass/
│   │   └── style.scss
│   └── js/
│       └── main.js
│
├── theme/                  # WordPress тема
│   ├── assets/            # Скомпилированные файлы (автоматически)
│   │   ├── css/
│   │   │   └── style.css  ← Из src/sass/
│   │   └── js/
│   │       └── main.js    ← Из src/js/
│   ├── inc/
│   ├── template-parts/
│   └── functions.php
│
├── vite.config.wordpress.mjs
└── package.json
```

---

## ⚙️ Команды

```bash
# Разработка с hot reload
npm run dev

# Production сборка
npm run build

# Production сборка с watch режимом
npm run build:watch

# Превью статики (если нужно)
npm run preview
```

---

## 🔧 Настройка путей

### Если тема в WordPress (через симлинк или копию):

В `vite.config.wordpress.mjs`:
```javascript
const WORDPRESS_THEME_PATH = resolve(__dirname, '../wp-content/themes/tochkagg-theme');
const ASSETS_PATH = WORDPRESS_THEME_PATH + '/assets';
```

Создайте симлинк (опционально):
```bash
cd /path/to/wordpress/wp-content/themes/
ln -s /path/to/Tochka-Gg/theme tochkagg-theme
```

### Если тема в этом же проекте:

В `vite.config.wordpress.mjs`:
```javascript
const LOCAL_THEME_PATH = resolve(__dirname, 'theme');
const ASSETS_PATH = LOCAL_THEME_PATH + '/assets';
```

---

## ✅ Проверка работы

### 1. Запустите Vite

```bash
npm run dev
```

Должно появиться:
```
  VITE v7.0.6  ready in XXX ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

### 2. Проверьте файлы

После запуска должны появиться файлы в:
- `theme/assets/css/style.css`
- `theme/assets/js/main.js`

### 3. Откройте WordPress

Откройте ваш локальный WordPress и проверьте, что стили и скрипты подключены.

---

## 🎯 Важные моменты

### 1. Пути в WordPress

В `functions.php` используйте правильные пути:

```php
wp_enqueue_style(
    'tochkagg-style',
    get_template_directory_uri() . '/assets/css/style.css',
    array(),
    filemtime(get_template_directory() . '/assets/css/style.css')
);

wp_enqueue_script(
    'tochkagg-script',
    get_template_directory_uri() . '/assets/js/main.js',
    array(),
    filemtime(get_template_directory() . '/assets/js/main.js'),
    true
);
```

### 2. Версионирование

Используйте `filemtime()` для автоматического обновления кеша при изменении файлов.

### 3. Hot Reload

Vite компилирует файлы → WordPress подхватывает → обновляйте страницу вручную.

Для автоматического обновления можно использовать Browser Sync или просто F5.

---

## ❓ FAQ

### Vite показывает только статическую страницу?

Это нормально! Vite работает только для фронтенда. Откройте WordPress в отдельной вкладке браузера.

### Файлы не компилируются?

1. Проверьте путь к теме в `vite.config.wordpress.mjs`
2. Проверьте, что папка `theme/assets/` существует
3. Проверьте консоль на ошибки

### Изменения не видны в WordPress?

1. Обновите страницу (F5 или Cmd+R)
2. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
3. Проверьте, что файлы скопировались в `theme/assets/`

### Нужен ли WordPress для разработки фронтенда?

Нет! Можете разрабатывать только CSS/JS через Vite, а PHP добавить позже.

---

## 🚀 Готово!

Теперь у вас настроена разработка WordPress темы с Vite.

**Для разработки:**
- Редактируете `src/sass/` и `src/js/`
- Vite автоматически компилирует
- Открываете WordPress и видите результат

**Для продакшена:**
```bash
npm run build
```

Всё! 🎉

