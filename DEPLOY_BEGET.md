# Инструкция по загрузке темы на Beget хостинг

## 📋 Что нужно сделать

### Шаг 1: Подготовка темы

1. Убедитесь, что все файлы скомпилированы:
   ```bash
   npm run build
   ```

2. Проверьте, что в папке `theme/assets/` есть файлы:
   - `css/style.css`
   - `js/main.js`

### Шаг 2: Структура на Beget

Обычно на Beget структура такая:
```
public_html/
├── wp-content/
│   └── themes/
│       └── tochkagg-theme/  ← сюда загружаем нашу тему
├── wp-config.php
└── ...
```

### Шаг 3: Загрузка темы

**Вариант 1: Через FTP (FileZilla, WinSCP)**
1. Подключитесь к хостингу по FTP
2. Перейдите в `public_html/wp-content/themes/`
3. Загрузите всю папку `theme/` и переименуйте её в `tochkagg-theme/`

**Вариант 2: Через файловый менеджер Beget**
1. Откройте файловый менеджер в панели Beget
2. Перейдите в `public_html/wp-content/themes/`
3. Загрузите архив темы и распакуйте

**Вариант 3: Через SSH (если доступен)**
```bash
# Загрузите файлы через SCP или создайте архив и загрузите
```

### Шаг 4: Активация темы

1. Войдите в админку WordPress
2. Внешний вид → Темы
3. Найдите "Tochka Gg" и активируйте

---

## ⚠️ Важные моменты

- Убедитесь, что папка темы называется `tochkagg-theme` (или как указано в WordPress)
- Все скомпилированные файлы должны быть в `theme/assets/`
- PHP файлы должны иметь правильные права доступа

---

**Покажите структуру вашего хостинга, и я подскажу точнее!**


