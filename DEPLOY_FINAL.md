# ✅ Финальная настройка деплоя на Beget

## 📍 Ваша структура

Путь на хостинге: `kungur-tochkagg.ru/public_html/wp-content/themes/`

В папке `themes/` сейчас:
- twentytwentyfive
- twentytwentyfour  
- twentytwentythree

---

## 🔧 Что нужно сделать

### 1. Создать папку для темы (один раз)

В файловом менеджере Beget:
1. Зайдите в `wp-content/themes/`
2. Создайте папку: `tochkagg-theme`
3. Права доступа: `755`

### 2. Проверить путь в deploy.yml

Я настроил путь как: `/public_html/wp-content/themes/tochkagg-theme/`

**На Beget FTP может подключаться по-разному:**

**Вариант A:** Если FTP подключается к корню (`/`)
- Путь: `/public_html/wp-content/themes/tochkagg-theme/` ✅ (уже установлено)

**Вариант B:** Если FTP подключается к `/public_html/`
- Нужно изменить на: `/wp-content/themes/tochkagg-theme/`

**Вариант C:** Если FTP подключается к домену
- Путь: `/wp-content/themes/tochkagg-theme/`

---

## 🚀 Первый запуск

1. **Создайте папку на хостинге** (см. выше)

2. **Сделайте push в GitHub:**
   ```bash
   git add .
   git commit -m "Setup auto deploy"
   git push
   ```

3. **Проверьте деплой:**
   - GitHub → Actions → должен запуститься workflow
   - Если ошибка с путем - скорректируйте в `.github/workflows/deploy.yml`

4. **Проверьте файлы:**
   - Зайдите в файловый менеджер
   - Должна появиться папка `tochkagg-theme/` с файлами

---

## ⚠️ Если путь неверный

Если деплой не работает из-за пути, попробуйте изменить в `.github/workflows/deploy.yml`:

```yaml
server-dir: /wp-content/themes/tochkagg-theme/  # если FTP в public_html
```

или

```yaml
server-dir: /kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/  # полный путь
```

---

## ✅ После успешного деплоя

1. Зайдите в WordPress админку
2. **Внешний вид → Темы**
3. Найдите "Tochka Gg" → **Активировать**

---

**Всё готово! Запустите первый деплой и проверьте!** 🚀

