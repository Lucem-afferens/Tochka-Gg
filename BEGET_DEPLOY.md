# Загрузка темы на Beget хостинг

## 📦 Подготовка темы к загрузке

### 1. Убедитесь что всё скомпилировано

```bash
npm run build
```

Проверьте что файлы есть:
- `theme/assets/css/style.css` ✅
- `theme/assets/js/main.js` ✅

---

## 📤 Загрузка на хостинг

### Вариант 1: Через файловый менеджер Beget (проще всего)

1. **Откройте файловый менеджер** в панели Beget

2. **Перейдите в папку тем:**
   ```
   public_html/wp-content/themes/
   ```

3. **Создайте папку для темы:**
   - Создайте папку: `tochkagg-theme`

4. **Загрузите все файлы из папки `theme/`:**
   - Загрузите все файлы и папки из `theme/` в `tochkagg-theme/`
   - Структура должна быть:
     ```
     tochkagg-theme/
     ├── style.css
     ├── functions.php
     ├── header.php
     ├── footer.php
     ├── assets/
     │   ├── css/
     │   └── js/
     ├── inc/
     ├── template-parts/
     └── ...
     ```

### Вариант 2: Через FTP (FileZilla, WinSCP)

1. Подключитесь к хостингу по FTP
2. Перейдите в `public_html/wp-content/themes/`
3. Загрузите всю папку `theme/` → переименуйте в `tochkagg-theme/`

---

## ⚙️ Активация темы

1. Войдите в админку WordPress: `kungur-tochkagg.ru/wp-admin`

2. Перейдите: **Внешний вид → Темы**

3. Найдите тему **"Tochka Gg"** и нажмите **"Активировать"**

---

## ✅ Проверка

После активации проверьте:
- Главная страница открывается
- Стили применяются (темная тема, неоновые цвета)
- Меню работает

---

## 🔧 Если что-то не работает

1. **Проверьте права доступа** - файлы должны быть 644, папки 755
2. **Проверьте что все файлы загружены** - особенно в `assets/`
3. **Очистите кеш** браузера (Ctrl+F5)

---

**Покажите структуру вашего хостинга, если нужна более точная инструкция!**

