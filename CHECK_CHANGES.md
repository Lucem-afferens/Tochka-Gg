# 🔍 Проверка изменений на сайте

## ✅ Что было изменено

Добавлены кибер-эффекты:
- Неоновое свечение кнопок при наведении
- Анимированные границы карточек
- Сканирующие линии
- Неоновое свечение текста
- Градиентные границы на карточках услуг

## 🔧 Как проверить изменения

### 1. Очистите кеш браузера

**Windows/Linux:**
- `Ctrl + Shift + R` (жесткая перезагрузка)
- Или `Ctrl + F5`

**Mac:**
- `Cmd + Shift + R` (жесткая перезагрузка)
- Или `Cmd + Option + E` (очистить кеш и перезагрузить)

**Или через DevTools:**
1. Откройте DevTools (F12)
2. Правой кнопкой на кнопку перезагрузки
3. Выберите "Очистить кеш и жестко перезагрузить"

### 2. Проверьте файл стилей

1. Откройте DevTools (F12)
2. Вкладка **Network**
3. Перезагрузите страницу
4. Найдите `style.css`
5. Проверьте размер: должно быть около **54 KB** (было меньше)

### 3. Проверьте конкретные элементы

**Кнопки:**
- Наведите на любую кнопку
- Должно появиться неоновое свечение
- Градиентный контур должен появиться

**Карточки услуг:**
- Наведите на карточку "Игровые ПК"
- Должна появиться неоновая граница
- Сканирующая линия должна пройти сверху вниз

**Заголовок Hero:**
- Наведите на заголовок "Точка Gg"
- Должно усилиться неоновое свечение текста

## 🚨 Если изменения не видны

### Вариант 1: Подождите деплой

1. Зайдите в GitHub → Actions
2. Проверьте последний деплой
3. Дождитесь завершения (обычно 1-2 минуты)

### Вариант 2: Принудительный деплой

Если деплой не запустился автоматически:

1. Зайдите в GitHub → Actions
2. Выберите workflow "🚀 Deploy to Beget"
3. Нажмите "Run workflow"
4. Выберите ветку `main`
5. Нажмите "Run workflow"

### Вариант 3: Проверьте сервер

Проверьте, что файлы на сервере обновлены:

1. Зайдите в файловый менеджер хостинга
2. Откройте: `wp-content/themes/tochkagg-theme/assets/css/style.css`
3. Проверьте дату изменения файла
4. Должна быть сегодняшняя дата

## 📊 Как узнать, что новые стили загрузились

Откройте консоль браузера (F12 → Console) и выполните:

```javascript
// Проверить размер CSS файла
fetch('/wp-content/themes/tochkagg-theme/assets/css/style.css')
  .then(r => r.text())
  .then(text => console.log('Размер CSS:', text.length, 'символов'));

// Должно быть около 55000+ символов
```

## ✅ После очистки кеша

Если вы очистили кеш и все ещё не видите изменения:
1. Подождите 2-3 минуты (кеш может обновляться)
2. Попробуйте другой браузер
3. Проверьте, что деплой завершился успешно

