# Работа с ACF без Options Page

## ✅ Да, можно работать без Options Page!

Вы можете создать обычные группы полей и привязать их к конкретным страницам или шаблонам.

---

## 🎯 Два подхода

### Подход 1: Группы полей на конкретных страницах (рекомендуется)

Создайте группы полей и привяжите их к конкретным страницам или шаблонам.

#### Пример: Группа полей для главной страницы

1. **Custom Fields → Field Groups → Add New**
2. **Group Title:** `Главная страница`
3. **Location Rules:**
   ```
   Show this field group if:
   Page Template → is equal to → Front Page
   ```
4. Добавьте поля (hero_title, hero_description и т.д.)
5. Нажмите **"Publish"**

Теперь эти поля будут доступны на главной странице, и вы можете использовать:
```php
$title = get_field('hero_title'); // Без 'option'!
```

---

### Подход 2: Группы полей для глобальных настроек

Для полей, которые используются с `'option'` в коде (логотип, контакты и т.д.), есть два варианта:

#### Вариант A: Создать страницу "Настройки" и привязать к ней

1. Создайте страницу WordPress: **Страницы → Добавить новую**
   - Название: `Настройки темы`
   - Сделайте её приватной (не публикуйте)

2. Создайте группу полей:
   - **Group Title:** `Глобальные настройки`
   - **Location Rules:**
     ```
     Show this field group if:
     Page → is equal to → Настройки темы
     ```

3. Добавьте все нужные поля (logo, phone_main, email_main и т.д.)

4. **ВАЖНО:** Нужно изменить код, чтобы использовать ID страницы вместо 'option':

```php
// Вместо:
$phone = get_field('phone_main', 'option');

// Используйте:
$settings_page_id = 123; // ID страницы "Настройки темы"
$phone = get_field('phone_main', $settings_page_id) ?: '8 (992) 222-62-72';
```

**Минусы:**
- Нужно править код во всех местах
- Нужно знать ID страницы

---

#### Вариант B: Использовать существующий код с 'option' (проще)

Если в коде уже используется `get_field('field_name', 'option')`, то:

1. **Создайте группу полей БЕЗ Location Rules** (или с очень широкими правилами)
2. В ACF есть возможность сохранять поля как "options" даже без Options Page

Но это работает только если:
- У вас установлен плагин "ACF Options Page"
- Или у вас ACF Pro

**Без плагина/Pro:**
- `get_field('field_name', 'option')` не будет работать
- Нужно использовать Вариант A (с ID страницы)

---

## 📋 Практический пример: Главная страница

### Создание группы полей для главной страницы:

1. **Custom Fields → Field Groups → Add New**
2. **Group Title:** `Главная страница`
3. **Location Rules:**
   ```
   Show this field group if:
   Page Template → is equal to → Front Page
   OR
   Front Page → is equal to → true
   ```
4. **Поля:**
   - `hero_title` (Text)
   - `hero_description` (Textarea)
   - `hero_background_type` (Select)
   - `hero_background_image` (Image)
   - `about_section_enabled` (True/False)
   - `about_title` (Text)
   - И так далее...

5. Нажмите **"Publish"**

6. Перейдите на главную страницу в админке
7. Прокрутите вниз - вы увидите ваши поля
8. Заполните их и сохраните

**В коде это уже работает!** Потому что используется `get_field('hero_title')` без 'option'.

---

## 📋 Практический пример: Страница контактов

1. **Custom Fields → Field Groups → Add New**
2. **Group Title:** `Страница контактов`
3. **Location Rules:**
   ```
   Show this field group if:
   Page Template → is equal to → Страница контактов
   ```
4. **Поля:**
   - `phone_main` (Text)
   - `email_main` (Email)
   - `address_full` (Textarea)
   - И так далее...

5. Нажмите **"Publish"**

6. Перейдите на страницу контактов
7. Заполните поля

**НО:** В коде используется `get_field('phone_main', 'option')`, поэтому нужно либо:
- Изменить код на `get_field('phone_main')` (для текущей страницы)
- Или создать Options Page

---

## ⚠️ Важный нюанс: 'option' vs обычные поля

### Разница:

```php
// Обычное поле на странице (без 'option')
$title = get_field('hero_title'); // Берется с текущей страницы

// Поле из Options Page (с 'option')
$phone = get_field('phone_main', 'option'); // Берется из глобальных настроек
```

### Если в коде используется 'option':

**Вариант 1:** Создать Options Page (удобно)
- Установить плагин "ACF Options Page"
- Создать группу полей с Location Rule на Options Page
- Код работает без изменений

**Вариант 2:** Изменить код (больше работы)
- Создать страницу "Настройки"
- Привязать группу полей к этой странице
- Изменить все `get_field('field', 'option')` на `get_field('field', $page_id)`
- Нужно править код во многих местах

---

## 🎯 Рекомендация

### Для полей главной страницы (hero_title, about_title и т.д.):
✅ **Создайте группу полей с Location Rule на Front Page**
- Код уже работает правильно
- Поля будут на главной странице
- Ничего менять не нужно

### Для глобальных настроек (logo, phone_main и т.д.):
✅ **Лучше создать Options Page** (через плагин)
- Код уже использует 'option'
- Не нужно ничего менять
- Удобное централизованное управление

❌ **Или изменить код** (если не хотите Options Page)
- Создать страницу "Настройки"
- Изменить все `get_field('field', 'option')` на `get_field('field', $page_id)`
- Больше работы, но работает

---

## 📝 Чек-лист работы без Options Page

### Шаг 1: Создайте группы полей для страниц

- [ ] Группа "Главная страница" → Location: Front Page
- [ ] Группа "Страница оборудования" → Location: Template Equipment
- [ ] Группа "Страница цен" → Location: Template Pricing
- [ ] Группа "Страница VR" → Location: Template VR
- [ ] Группа "Страница бара" → Location: Template Bar
- [ ] Группа "Страница бронирования" → Location: Template Booking
- [ ] Группа "Страница контактов" → Location: Template Contacts

### Шаг 2: Для глобальных настроек

**Вариант A (рекомендуется):**
- [ ] Установить плагин "ACF Options Page"
- [ ] Создать группу "Глобальные настройки" → Location: Options Page

**Вариант B (если не хотите Options Page):**
- [ ] Создать страницу "Настройки темы"
- [ ] Создать группу "Глобальные настройки" → Location: Page "Настройки темы"
- [ ] Изменить код: заменить все `'option'` на ID страницы

---

## ✅ Вывод

**Да, можно работать без Options Page!**

1. **Для полей страниц** - просто создайте группы полей с Location Rules на нужные страницы/шаблоны
2. **Для глобальных настроек** - либо установите плагин для Options Page, либо измените код

**Самый простой путь:**
- Создайте группы полей для каждой страницы
- Для глобальных настроек установите плагин "ACF Options Page" (это займет 2 минуты)

