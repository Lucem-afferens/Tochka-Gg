# 🚀 Инструкция по установке и настройке

## ✅ Что уже сделано автоматически

Создана полная структура WordPress темы со всеми файлами:

- ✅ **Структура папок** - все директории созданы
- ✅ **PHP файлы темы** - functions.php, header.php, footer.php, index.php и др.
- ✅ **Модульная структура** - все файлы в `inc/` разделены логически
- ✅ **SASS структура** - все переменные из дизайн-системы готовы
- ✅ **JavaScript** - базовые модули навигации
- ✅ **Vite конфигурация** - настроена под WordPress тему
- ✅ **Template parts** - базовые компоненты

---

## 📋 Что нужно сделать ВАМ

### 1. Установить npm зависимости

```bash
npm install
```

**Это единственная команда, которую нужно запустить вручную.**

---

## 🎯 После установки - все готово!

### Запуск разработки

```bash
npm run dev
```

Vite будет:
- ✅ Компилировать SASS → CSS в `theme/assets/css/style.css`
- ✅ Компилировать JS → в `theme/assets/js/main.js`
- ✅ Автоматически пересобирать при изменениях
- ✅ Работать на http://localhost:3000 (но вы будете использовать WordPress)

---

## 📁 Структура проекта

```
Tochka-Gg/
├── src/                          # Исходники (редактируете здесь)
│   ├── sass/
│   │   ├── style.scss           # Главный файл стилей
│   │   ├── base/                # Переменные, reset, mixins
│   │   ├── components/          # Кнопки, карточки
│   │   ├── sections/            # Header, footer
│   │   └── utilities/           # Helpers, анимации
│   └── js/
│       └── main.js              # Главный JS файл
│
├── theme/                        # WordPress тема (готова!)
│   ├── assets/                  # Скомпилированные файлы (автоматически)
│   │   ├── css/
│   │   │   └── style.css        ← Из src/sass/
│   │   └── js/
│   │       └── main.js          ← Из src/js/
│   ├── inc/                     # PHP модули
│   ├── template-parts/          # Компоненты
│   ├── style.css                # Заголовок темы
│   └── functions.php            # Инициализация
│
├── vite.config.wordpress.mjs    # Конфигурация Vite
└── package.json
```

---

## 🔧 Настройка (если нужно)

### Если тема в WordPress (не в этом проекте)

1. Откройте `vite.config.wordpress.mjs`
2. Измените путь:
   ```javascript
   const ASSETS_PATH = WORDPRESS_THEME_PATH + '/assets';
   ```
3. Укажите правильный путь к WordPress теме

### Если используете симлинк

```bash
cd /path/to/wordpress/wp-content/themes/
ln -s /Users/nikolai/Desktop/www/Tochka-Gg/theme tochkagg-theme
```

---

## 🎨 Процесс разработки

### 1. Запустить Vite

```bash
npm run dev
```

### 2. Редактировать файлы

**SASS:**
- Редактируете `src/sass/`
- Vite автоматически компилирует → `theme/assets/css/style.css`

**JavaScript:**
- Редактируете `src/js/`
- Vite автоматически компилирует → `theme/assets/js/main.js`

**PHP/Шаблоны:**
- Редактируете файлы в `theme/`
- Обновляете страницу в WordPress → видите изменения

---

## 📝 Настройка WordPress

### После установки WordPress:

1. **Скопировать тему:**
   - Скопировать папку `theme/` в `wp-content/themes/tochkagg-theme`
   - Или создать симлинк (см. выше)

2. **Активировать тему:**
   - WordPress → Внешний вид → Темы → Активировать "Tochka Gg"

3. **Установить ACF Pro:**
   - Установить плагин Advanced Custom Fields Pro
   - Создать Options Page для глобальных настроек
   - Настроить поля согласно `STRUCTURE.md`

4. **Создать меню:**
   - Внешний вид → Меню
   - Создать меню и назначить в "Главное меню"

---

## ⚡ Команды

```bash
# Разработка (hot reload)
npm run dev

# Production сборка
npm run build

# Production сборка с watch
npm run build:watch
```

---

## ✅ Проверка работоспособности

### 1. Проверить что Vite запускается:

```bash
npm run dev
```

Должно появиться:
```
VITE v7.0.6  ready in XXX ms

➜  Local:   http://localhost:3000/
```

### 2. Проверить что файлы компилируются:

После запуска `npm run dev` должны появиться:
- `theme/assets/css/style.css`
- `theme/assets/js/main.js`

### 3. Открыть WordPress:

Откройте ваш локальный WordPress и проверьте:
- Тема активирована
- Стили подключены
- Скрипты работают

---

## 🎯 Готово к работе!

**Всё настроено!** Теперь просто:

1. Запустите `npm install` (один раз)
2. Запустите `npm run dev`
3. Начните разработку!

---

## 📚 Дополнительные файлы

- `STRUCTURE.md` - полная структура темы
- `DESIGN_SYSTEM.md` - дизайн-система
- `CLUB_CHARACTERISTICS.md` - характеристики клуба
- `WORKFLOW.md` - подробный workflow
- `QUICKSTART.md` - быстрый старт

---

**Если что-то не работает, напишите - помогу настроить!** 🚀
