# 🔧 Исправление: Шаблоны не появляются в списке

## ❌ Проблема

При создании/редактировании страницы в выпадающем списке "Шаблон" нет наших шаблонов.

---

## ✅ Решение 1: Проверка файлов на сервере

### Проверьте, что файлы шаблонов есть на сервере:

Шаблоны должны быть в папке темы:
- `wp-content/themes/tochkagg-theme/template-equipment.php`
- `wp-content/themes/tochkagg-theme/template-vr.php`
- `wp-content/themes/tochkagg-theme/template-contacts.php`
- `wp-content/themes/tochkagg-theme/template-pricing.php`

**Проверьте через файловый менеджер или FTP!**

---

## ✅ Решение 2: Очистка кеша

1. **Перезагрузите страницу в админке:**
   - Нажмите **F5** или **Ctrl+R**
   - Иногда WordPress кеширует список шаблонов

2. **Очистите кеш WordPress:**
   - Если установлен плагин кеширования — очистите его
   - Попробуйте переключить тему и вернуться обратно

---

## ✅ Решение 3: Принудительная установка шаблона через код

Если шаблоны не появляются, можно установить шаблон напрямую через код:

### Шаг 1: Узнайте ID страницы

1. Откройте страницу "Оборудование" для редактирования
2. Посмотрите на URL в адресной строке:
   - Пример: `post.php?post=123&action=edit`
   - Число **123** — это ID страницы

### Шаг 2: Добавьте код в functions.php

1. Откройте файл `theme/inc/theme-functions.php`
2. Найдите строку:
   ```php
   // add_action('admin_init', 'tochkagg_force_set_equipment_template');
   ```
3. Раскомментируйте её (уберите `//`):
   ```php
   add_action('admin_init', 'tochkagg_force_set_equipment_template');
   ```
4. Найдите строку:
   ```php
   $page_id = 0; // <-- УКАЖИТЕ ID СТРАНИЦЫ ЗДЕСЬ
   ```
5. Замените `0` на реальный ID страницы:
   ```php
   $page_id = 123; // <-- УКАЖИТЕ ID СТРАНИЦЫ ЗДЕСЬ
   ```

### Шаг 3: Загрузите файл на сервер

1. Сохраните файл
2. Загрузите на сервер через FTP или файловый менеджер
3. Зайдите в админку WordPress (это запустит функцию)
4. **Сразу закомментируйте строку обратно:**
   ```php
   // add_action('admin_init', 'tochkagg_force_set_equipment_template');
   ```
5. И установите `$page_id = 0;` обратно

---

## ✅ Решение 4: Проверка заголовков шаблонов

Убедитесь, что в начале каждого файла шаблона есть правильный заголовок:

```php
<?php
/**
 * Template Name: Страница оборудования
 */
```

**Проверьте все 4 файла шаблонов!**

---

## ✅ Решение 5: Установка шаблона через базу данных

Если ничего не помогает, можно установить шаблон напрямую в базе данных:

1. Откройте базу данных WordPress (через phpMyAdmin)
2. Найдите таблицу `wp_postmeta`
3. Найдите запись где:
   - `post_id` = ID вашей страницы
   - `meta_key` = `_wp_page_template`
4. Измените `meta_value` на:
   - `template-equipment.php` (для страницы Оборудование)
   - `template-vr.php` (для страницы VR арена)
   - `template-contacts.php` (для страницы Контакты)
   - `template-pricing.php` (для страницы Цены)

---

## 🔍 Диагностика

### Проверьте, видит ли WordPress шаблоны:

1. Зайдите в админку
2. Откройте консоль браузера (F12)
3. Попробуйте создать страницу
4. Посмотрите, есть ли ошибки JavaScript

### Проверьте права доступа:

Файлы шаблонов должны быть:
- Читаемыми (права: `644`)
- Принадлежать правильному пользователю

---

## 📝 Быстрая проверка

Выполните все шаги по порядку:
1. ✅ Файлы на месте?
2. ✅ Перезагрузили страницу?
3. ✅ Попробовали установить через код?
4. ✅ Проверили заголовки шаблонов?

---

**Начните с Решения 1 и 2, они самые простые!**

