# 📁 Создание папок на сервере

## ❌ Проблема

Файл загружается в корень темы:
```
tochkagg-theme/style.css  ❌
```

Вместо правильной структуры:
```
tochkagg-theme/assets/css/style.css  ✅
```

---

## ✅ Решение: Создать папки вручную

FTP может не создавать подпапки автоматически. Нужно создать структуру папок на сервере:

### Шаг 1: Создайте папки на сервере

**Через файловый менеджер Beget:**

1. Зайдите в: `kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/`
2. Создайте папку: `assets`
   - Права: `755`
3. Внутри `assets/` создайте папку: `css`
   - Права: `755`
4. Внутри `assets/` создайте папку: `js`
   - Права: `755`

**Итоговая структура должна быть:**
```
tochkagg-theme/
  ├── assets/
  │   ├── css/
  │   └── js/
  ├── functions.php
  ├── style.css
  └── ...
```

---

### Шаг 2: Проверьте после деплоя

После следующего деплоя проверьте:

1. **Файл должен быть в правильном месте:**
   - `tochkagg-theme/assets/css/style.css` ✅
   - НЕ в корне темы ❌

2. **Проверьте через браузер:**
   ```
   http://kungur-tochkagg.ru/wp-content/themes/tochkagg-theme/assets/css/style.css
   ```

---

## 🔍 Если папки уже есть

Если папки `assets/css/` уже существуют на сервере, но файл все равно загружается в корень, проблема может быть в:

1. **Права доступа** — проверьте права на папки (должно быть 755)
2. **Путь FTP** — возможно, путь указан неправильно
3. **Структура локально** — убедитесь, что локально файл в `theme/assets/css/`

---

**Создайте папки на сервере и сделайте еще один деплой - файлы должны загрузиться в правильные места!**

