# Настройка автоматического деплоя на Beget

## 🚀 Автоматический деплой через GitHub Actions

При каждом push в GitHub тема автоматически соберется и загрузится на хостинг.

---

## 📋 Шаг 1: Создать репозиторий на GitHub

1. Создайте новый репозиторий на GitHub
2. Инициализируйте git в проекте (если еще не сделано):

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_АККАУНТ/Tochka-Gg.git
git push -u origin main
```

---

## 🔐 Шаг 2: Настроить секреты в GitHub

### Вариант A: Через FTP (проще)

1. В репозитории GitHub: **Settings → Secrets and variables → Actions**
2. Добавьте секреты:
   - `BEGET_FTP_HOST` - FTP хост (например: `kungur-tochkagg.ru` или IP)
   - `BEGET_FTP_USER` - FTP пользователь
   - `BEGET_FTP_PASSWORD` - FTP пароль

### Вариант B: Через SFTP/SSH (безопаснее)

1. В репозитории GitHub: **Settings → Secrets and variables → Actions**
2. Добавьте секреты:
   - `BEGET_SSH_KEY` - приватный SSH ключ
   - `BEGET_SSH_HOST` - SSH хост
   - `BEGET_SSH_USER` - SSH пользователь
   - `BEGET_SSH_PORT` - SSH порт (обычно 22)

---

## ⚙️ Шаг 3: Проверить настройки

Файлы уже созданы:
- `.github/workflows/deploy.yml` - для FTP
- `.github/workflows/deploy-sftp.yml` - для SFTP

Выберите нужный вариант (FTP или SFTP).

---

## 🔧 Шаг 4: Настроить пути

### Узнайте точный путь на Beget:

Обычно путь такой:
```
/public_html/wp-content/themes/tochkagg-theme/
```

Но на Beget может быть:
- `/home/u/user/kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/`

**Покажите точный путь в файловом менеджере Beget!**

---

## ✅ Шаг 5: Протестировать

1. Сделайте изменения в коде
2. Запушите в GitHub:
   ```bash
   git add .
   git commit -m "Test deploy"
   git push
   ```

3. Проверьте Actions в GitHub - должен запуститься деплой

---

## 📝 Что деплоится

При каждом push автоматически:
1. ✅ Устанавливаются зависимости (`npm ci`)
2. ✅ Собираются файлы (`npm run build`)
3. ✅ Загружаются на хостинг только файлы из `theme/`

---

## ⚠️ Важно

- Убедитесь что папка `tochkagg-theme/` существует на хостинге
- Проверьте права доступа (755 для папок, 644 для файлов)
- Первый раз загрузите тему вручную, потом будет автоматически

---

**Покажите структуру вашего хостинга, и я настрою точные пути!**


