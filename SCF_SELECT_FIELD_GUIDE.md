# 📋 Как создать поле Select (Выпадающий список) в SCF

## Проблема
В SCF поле типа "Select" работает иначе, чем в ACF — варианты вводятся построчно в текстовое поле, а не через кнопку "Добавить выбор".

---

## ✅ Правильный способ создания поля Select

### Шаг 1: Основные настройки

1. **Метка поля**: `Тип фона Hero`
2. **Название поля**: `hero_background_type`
3. **Тип поля**: Выберите **"Select"**

### Шаг 2: Настройка вариантов (Choices)

В поле **"Варианты"** (Choices) введите варианты построчно (каждый вариант с новой строки):

#### Вариант 1: Только значения (простой способ)
```
image
video
```

#### Вариант 2: Значение и метка (рекомендуется)
```
image : Изображение
video : Видео
```

**Формат**: `значение : Метка` (двоеточие с пробелом)

### Шаг 3: Значение по умолчанию

В поле **"Значение по умолчанию"** введите одно значение:
```
image
```

### Шаг 4: Возвращаемый формат

Выберите **"Значение"** (Value) — это вернет `image` или `video` (то, что слева от двоеточия)

---

## 📝 Примеры для разных случаев

### Пример 1: Простые варианты без меток
```
red
blue
green
```

### Пример 2: Варианты с метками
```
red : Красный
blue : Синий
green : Зеленый
```

### Пример 3: Варианты с числами
```
1 : Первый вариант
2 : Второй вариант
3 : Третий вариант
```

### Пример 4: Варианты для типа фона Hero
```
image : Изображение
video : Видео
```

---

## ⚠️ Важные моменты

1. **Каждый вариант с новой строки** — не разделяйте запятыми
2. **Двоеточие с пробелом** — формат `значение : Метка`
3. **Значение по умолчанию** — только одно значение (без метки)
4. **Возвращаемый формат** — выберите "Значение" для получения `image`/`video`, или "Этикетка" для получения "Изображение"/"Видео"

---

## 🎯 Для поля "Тип фона Hero"

**Точные настройки:**

1. **Метка**: `Тип фона Hero`
2. **Название**: `hero_background_type`
3. **Тип**: `Select`
4. **Варианты**:
   ```
   image : Изображение
   video : Видео
   ```
5. **Значение по умолчанию**: `image`
6. **Возвращаемый формат**: `Значение`
7. **Множественный выбор**: НЕ включайте (оставьте выключенным)

---

## ❓ Часто задаваемые вопросы

### Q: Что будет сохранено в базе данных?

**A**: Если выбран формат "Значение", сохранится `image` или `video`. Если "Этикетка" — сохранится "Изображение" или "Видео".

### Q: Можно ли использовать только значения без меток?

**A**: Да, можно просто написать:
```
image
video
```
В этом случае и значение, и метка будут одинаковыми.

### Q: Что выбрать в "Возвращаемый формат"?

**A**: 
- **"Значение"** — вернет `image` или `video` (рекомендуется для кода)
- **"Этикетка"** — вернет "Изображение" или "Видео" (для отображения пользователю)
- **"Оба (массив)"** — вернет массив с обоими значениями

---

**Нужна помощь?** См. инструкцию `SCF_HERO_SECTION_SETUP.md` для полной настройки Hero секции

