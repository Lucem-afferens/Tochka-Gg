# 🔧 Исправление расположения файлов при деплое

## ❌ Проблема

Файл `style.css` загружается в корень темы:
```
tochkagg-theme/style.css  ❌ (неправильно)
```

Вместо правильного места:
```
tochkagg-theme/assets/css/style.css  ✅ (правильно)
```

---

## ✅ Решение

Проблема в том, что FTP деплой не сохраняет структуру папок. Нужно убедиться, что:

1. **Локальная структура правильная:**
   ```
   theme/
     ├── assets/
     │   ├── css/
     │   │   └── style.css
     │   └── js/
     │       └── main.js
     ├── functions.php
     └── ...
   ```

2. **На сервере должны быть созданы папки:**
   - `tochkagg-theme/assets/`
   - `tochkagg-theme/assets/css/`
   - `tochkagg-theme/assets/js/`

3. **Файлы должны загружаться в правильные папки**

---

## 🔍 Что проверить

1. **На сервере проверьте структуру:**
   - Есть ли папка `assets/css/`?
   - Где находится файл `style.css`?

2. **В логах деплоя:**
   - Какие файлы загружаются?
   - В какие папки они попадают?

---

## 📝 Варианты решения

### Вариант 1: Создать папки на сервере вручную

1. Зайдите в файловый менеджер
2. Создайте структуру:
   ```
   tochkagg-theme/
     └── assets/
         ├── css/
         └── js/
   ```
3. Права: `755` на все папки

### Вариант 2: Проверить настройки FTP деплоя

Возможно, нужно добавить опцию для создания папок автоматически.

---

**Сообщите:**
1. Есть ли на сервере папка `assets/css/`?
2. Где именно находится файл `style.css`?

После этого я смогу точно исправить проблему!

