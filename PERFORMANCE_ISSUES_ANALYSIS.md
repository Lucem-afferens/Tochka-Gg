# 🔍 Анализ проблем производительности сайта

## 📊 Общая статистика

- **CSS эффекты**: 844 использования (will-change, filter, backdrop-filter, box-shadow, text-shadow, animation, transform, transition)
- **JavaScript обработчики**: 41 использование (requestAnimationFrame, addEventListener scroll, setTimeout, setInterval)

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **CSS: Тяжелые эффекты на десктопе**

#### Проблема 1.1: `backdrop-filter` на header
**Файл**: `src/sass/sections/_header.scss`
**Строки**: 18-19, 69-70
**Проблема**: 
- `backdrop-filter: blur(12px) saturate(180%)` применяется постоянно
- При скролле увеличивается до `blur(15px) saturate(200%)`
- Это один из самых тяжелых CSS эффектов, особенно на десктопе

**Решение**:
- Использовать `backdrop-filter` только на мобильных (где он нужен для меню)
- На десктопе использовать обычный `background` с прозрачностью
- Или использовать `contain: layout style paint` для изоляции

**Приоритет**: 🔴 КРИТИЧЕСКИЙ

---

#### Проблема 1.2: Множественные `text-shadow` слои
**Файл**: `src/sass/sections/_hero.scss`
**Строки**: 312-318, 333-340, 434-458, 476-501
**Проблема**:
- До 6 слоев `text-shadow` на одном элементе
- Анимация изменяет все слои одновременно
- Каждый слой требует перерисовки

**Решение**:
- Уменьшить количество слоев до 2-3 максимум
- Использовать `text-shadow` только для основного свечения
- Остальные эффекты через `filter: drop-shadow()` (более производительно)
- Или использовать SVG фильтры для сложных эффектов

**Приоритет**: 🔴 КРИТИЧЕСКИЙ

---

#### Проблема 1.3: Множественные `box-shadow` слои
**Файл**: `src/sass/sections/_header.scss`, `src/sass/utilities/_animations.scss`
**Строки**: Множественные
**Проблема**:
- До 8 слоев `box-shadow` на одном элементе (header.scrolled)
- Анимации изменяют все слои одновременно
- Тяжелые вычисления при каждом кадре

**Решение**:
- Уменьшить количество слоев до 3-4 максимум
- Использовать `box-shadow` только для основного эффекта
- Остальные эффекты через псевдоэлементы с отдельными тенями
- Или использовать `filter: drop-shadow()` для дополнительных эффектов

**Приоритет**: 🟡 ВЫСОКИЙ

---

#### Проблема 1.4: `filter` в анимациях
**Файл**: `src/sass/utilities/_animations.scss`
**Строки**: 56-77, 428-469, 472-522
**Проблема**:
- `filter: hue-rotate()`, `filter: brightness()` в keyframes
- `filter` вызывает перерисовку всего элемента и его потомков
- Особенно тяжело при анимации

**Решение**:
- Убрать `filter` из анимаций, использовать только `opacity` и `transform`
- Если нужны цветовые эффекты - использовать CSS переменные с `color`
- Или использовать `mix-blend-mode` вместо `filter`

**Приоритет**: 🟡 ВЫСОКИЙ

---

### 2. **JavaScript: Обработчики скролла**

#### Проблема 2.1: `setActiveMenuItem` вызывается при каждом скролле
**Файл**: `src/js/modules/navigation.js`
**Строки**: 168-232, 282-297
**Проблема**:
- `setActiveMenuItem()` вызывается с `setTimeout(150ms)` при каждом скролле
- Функция делает `querySelectorAll`, `getBoundingClientRect`, `classList` операции
- Выполняется даже когда меню не видно или нет якорных ссылок

**Решение**:
- Увеличить debounce до 300-500ms
- Проверять, есть ли якорные ссылки ДО вызова функции
- Кэшировать результаты `getBoundingClientRect`
- Использовать `IntersectionObserver` вместо проверки при скролле

**Приоритет**: 🔴 КРИТИЧЕСКИЙ

---

#### Проблема 2.2: Множественные `requestAnimationFrame` вызовы
**Файл**: `src/js/modules/navigation.js`, `src/js/modules/animations.js`
**Строки**: 303-309, 60-66
**Проблема**:
- Каждый модуль создает свой `requestAnimationFrame` цикл
- Нет координации между модулями
- Может быть до 3-4 RAF вызовов на один кадр

**Решение**:
- Объединить все RAF вызовы в один цикл
- Использовать единый менеджер анимаций
- Или использовать `IntersectionObserver` вместо RAF для скролл-анимаций

**Приоритет**: 🟡 ВЫСОКИЙ

---

#### Проблема 2.3: Синхронизация высоты при каждом resize
**Файл**: `src/js/modules/news-cards.js`, `src/js/modules/slider.js`
**Строки**: 57-63, 110-118, 167-170
**Проблема**:
- `syncNewsCardsHeight()` вызывается при каждом resize с debounce 250ms
- `syncSlideHeights()` вызывается при каждом resize с debounce 250ms
- Обе функции делают `offsetHeight` чтения (force reflow)

**Решение**:
- Увеличить debounce до 500ms
- Использовать `ResizeObserver` вместо `resize` события
- Кэшировать результаты измерений
- Выполнять синхронизацию только при реальном изменении размера

**Приоритет**: 🟡 ВЫСОКИЙ

---

### 3. **Анимации и will-change**

#### Проблема 3.1: Неоптимальное использование `will-change`
**Файл**: `src/sass/utilities/_animations.scss`, `src/sass/sections/_hero.scss`
**Строки**: 662-685, 291
**Проблема**:
- `will-change` установлен постоянно для многих элементов
- Не сбрасывается после анимации
- Может вызывать излишнее использование GPU памяти

**Решение**:
- Устанавливать `will-change` только перед анимацией
- Сбрасывать после завершения анимации
- Использовать только для элементов, которые действительно анимируются
- На десктопе можно использовать меньше `will-change`

**Приоритет**: 🟡 ВЫСОКИЙ

---

#### Проблема 3.2: Множественные одновременные анимации
**Файл**: `src/sass/utilities/_animations.scss`, `src/sass/sections/_header.scss`
**Проблема**:
- На одной странице может быть 10+ анимируемых элементов одновременно
- Каждая анимация требует вычислений
- Особенно тяжело на десктопе с большим количеством элементов

**Решение**:
- Отключать анимации для элементов вне viewport
- Использовать `IntersectionObserver` для включения анимаций только при появлении
- Упростить анимации на десктопе (меньше слоев, проще эффекты)
- Использовать `prefers-reduced-motion` более агрессивно

**Приоритет**: 🟡 ВЫСОКИЙ

---

### 4. **Специфичные проблемы десктопа**

#### Проблема 4.1: Все оптимизации для мобильных, десктоп не оптимизирован
**Проблема**:
- Код оптимизирован для мобильных устройств
- На десктопе используются те же тяжелые эффекты
- Десктоп имеет больше элементов на экране, но использует те же эффекты

**Решение**:
- Создать отдельные оптимизации для десктопа
- Упростить эффекты на десктопе (меньше blur, меньше теней)
- Использовать `@media (min-width: 1024px)` для упрощения эффектов
- Отключить некоторые анимации на десктопе, если они не критичны

**Приоритет**: 🔴 КРИТИЧЕСКИЙ

---

#### Проблема 4.2: Тяжелые градиенты с анимацией
**Файл**: `src/sass/sections/_hero.scss`, `src/sass/sections/_header.scss`
**Проблема**:
- Градиенты с `background-size: 200% 200%` и анимацией `background-position`
- Анимация градиента требует перерисовки всего элемента
- Особенно тяжело на больших элементах (hero, header)

**Решение**:
- Упростить градиенты на десктопе
- Использовать статичные градиенты вместо анимированных
- Или использовать `transform: scale()` вместо `background-position` для анимации

**Приоритет**: 🟡 ВЫСОКИЙ

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Этап 1: Критические CSS оптимизации (приоритет 1)
1. ✅ Упростить `backdrop-filter` на десктопе
2. ✅ Уменьшить количество `text-shadow` слоев
3. ✅ Уменьшить количество `box-shadow` слоев
4. ✅ Убрать `filter` из анимаций

### Этап 2: JavaScript оптимизации (приоритет 2)
1. ✅ Оптимизировать `setActiveMenuItem` с лучшим debounce
2. ✅ Объединить RAF вызовы
3. ✅ Оптимизировать синхронизацию высоты

### Этап 3: Анимации и will-change (приоритет 3)
1. ✅ Оптимизировать использование `will-change`
2. ✅ Отключать анимации для элементов вне viewport
3. ✅ Упростить анимации на десктопе

### Этап 4: Специфичные оптимизации десктопа (приоритет 4)
1. ✅ Создать отдельные оптимизации для десктопа
2. ✅ Упростить градиенты на десктопе

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

- **FPS на десктопе**: с 30-40 до 55-60
- **Время отклика на скролл**: с 50-100ms до 10-20ms
- **Использование GPU**: снижение на 30-40%
- **Плавность анимаций**: улучшение на 50-70%

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Не ломать логику**: Все изменения должны сохранять функциональность
2. **Не менять дизайн**: Визуально сайт должен остаться таким же
3. **Мобильные не трогать**: Оптимизации только для десктопа (min-width: 1024px)
4. **Постепенное внедрение**: Тестировать каждое изменение отдельно

