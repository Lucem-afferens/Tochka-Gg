name: 🚀 Deploy to Beget

on:
  push:
    branches:
      - main
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    # Это гарантирует, что статус будет виден рядом с коммитом
    outputs:
      status: ${{ job.status }}
    
    # Убеждаемся, что статус показывается рядом с коммитом
    permissions:
      contents: read
      deployments: write
      statuses: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2 # Для проверки изменений
      
      - name: Check if deployment needed
        id: check
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "deploy=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # Проверяем изменения в нужных файлах
          changed_files=$(git diff --name-only HEAD~1 HEAD | grep -E '^(theme/|src/|vite.config.wordpress.mjs|package.json|\.github/workflows/deploy\.yml)' || true)
          
          if [ -z "$changed_files" ]; then
            echo "deploy=false" >> $GITHUB_OUTPUT
            echo "⏭️ No changes in theme or source files, skipping deployment"
          else
            echo "deploy=true" >> $GITHUB_OUTPUT
            echo "✅ Changes detected, deployment will proceed"
          fi
      
      - name: Setup Node.js
        if: steps.check.outputs.deploy == 'true'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        if: steps.check.outputs.deploy == 'true'
        run: npm ci
      
      - name: Build assets
        if: steps.check.outputs.deploy == 'true'
        run: npm run build
      
      - name: Check files before deploy
        if: steps.check.outputs.deploy == 'true'
        run: |
          echo "📦 Checking files to deploy:"
          echo ""
          echo "CSS file:"
          if [ -f "theme/assets/css/style.css" ]; then
            ls -lh theme/assets/css/style.css
            CSS_SIZE=$(wc -c < theme/assets/css/style.css)
            echo "CSS file size: $CSS_SIZE bytes (~$((CSS_SIZE/1024)) KB)"
            if [ "$CSS_SIZE" -lt 50000 ]; then
              echo "⚠️ WARNING: CSS file seems too small! Expected ~54KB"
            fi
          else
            echo "❌ CSS file not found!"
            exit 1
          fi
          echo ""
          echo "JS file:"
          if [ -f "theme/assets/js/main.js" ]; then
            ls -lh theme/assets/js/main.js
          else
            echo "⚠️ JS file not found!"
          fi
          echo ""
          echo "Checking CSS content:"
          if grep -q "tgg-cyber-bg\|cyber-effects" theme/assets/css/style.css 2>/dev/null; then
            echo "✅ Cyber effects found in CSS"
          else
            echo "⚠️ Cyber effects not found in CSS"
          fi
          echo ""
          echo "✅ Files ready for deployment"
      
      - name: Deploy to Beget via FTP
        if: steps.check.outputs.deploy == 'true'
        id: deploy
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ${{ secrets.BEGET_FTP_HOST }}
          username: ${{ secrets.BEGET_FTP_USER }}
          password: ${{ secrets.BEGET_FTP_PASSWORD }}
          # Путь на Beget: от корня FTP подключения до папки темы
          # Попробуем несколько вариантов пути:
          # Вариант 1: если FTP подключается к корню домена
          server-dir: /public_html/wp-content/themes/tochkagg-theme/
          local-dir: ./theme/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.DS_Store
            **/Thumbs.db
          dangerous-clean-slate: false
          dry-run: false
          log-level: verbose
      
      - name: Deployment summary
        if: always()
        run: |
          if [ "${{ steps.deploy.outcome }}" == "success" ]; then
            echo "✅ Deployment successful!"
          else
            echo "❌ Deployment failed! Check logs above for details."
            exit 1
          fi
