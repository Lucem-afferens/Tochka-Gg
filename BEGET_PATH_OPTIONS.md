# Варианты путей для Beget

## 🔍 Как определить правильный путь

На Beget путь зависит от того, **куда подключается FTP**.

### Вариант 1: FTP подключается к корню домена
Если при подключении вы попадаете в `/kungur-tochkagg.ru/public_html/`:
```yaml
server-dir: /kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/
```

### Вариант 2: FTP подключается к `/public_html/`
Если при подключении вы сразу видите `wp-content/`:
```yaml
server-dir: /wp-content/themes/tochkagg-theme/
```

### Вариант 3: FTP подключается к корню сервера
Если видите домашнюю папку с несколькими доменами:
```yaml
server-dir: /home/u1234567/kungur-tochkagg.ru/public_html/wp-content/themes/tochkagg-theme/
```

---

## ✅ Как проверить

1. Подключитесь к FTP (FileZilla или через панель Beget)
2. Посмотрите, в какой папке вы оказались
3. Найдите путь до `wp-content/themes/`
4. Обновите `server-dir` в `.github/workflows/deploy.yml`

---

## 🚀 Быстрый тест

Сделайте первый push и посмотрите логи в GitHub Actions:
- Если ошибка "directory not found" - путь неверный
- Если ошибка "permission denied" - проблемы с правами
- Если всё OK - файлы загрузятся!

---

**Сейчас установлен путь для варианта 1. Если не работает - попробуйте вариант 2!**

